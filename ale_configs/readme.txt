Example config files intented to be stored in project directories as "config.vim"
Can source (:source config.vim) after opening vim to apply settings
These scripts can choose specific linters, settings, etc for the project
